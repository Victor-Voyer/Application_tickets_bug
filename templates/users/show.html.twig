{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
    <h1>Profile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Pseudo:</th>
                <td>{{ user.nickname }}</td>
            </tr>
            <tr>
                <th>Nom:</th>
                <td>{{ user.firstName }}</td>
            </tr>
            <tr>
                <th>Prénom:</th>
                <td>{{ user.lastName }}</td>
            </tr>
            <tr>
                <th>Email:</th>
                <td>{{ user.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tickets_index') }}">Back to Tickets</a>

    <a href="{{ path('app_users_edit', {'id': user.id}) }}">Edit Account</a>

    {{ include('users/_delete_form.html.twig') }}
{% endblock %}

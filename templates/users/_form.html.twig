{{ form_start(form) }}
{{ form_widget(form) }}
    <div class="form-buttons">
        <button class="btn btn-primary">{{ button_label|default('Submit') }}</button>
        <a class="btn btn-secondary" href="{{ path('app_users_index') }}">Back to Home</a>
    </div>
{{ form_end(form) }}
